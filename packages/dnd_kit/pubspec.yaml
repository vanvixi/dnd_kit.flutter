name: dnd_kit
description: Flutter drag-and-drop toolkit with core widgets, sensors, measuring, overlays, and sortable presets.
version: 0.1.0-dev.0
publish_to: none
repository: https://github.com/vanvixi/dnd_kit.flutter
issue_tracker: https://github.com/vanvixi/dnd_kit.flutter/issues
topics:
  - drag-and-drop
  - dnd
  - flutter
  - sortable

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  dnd_kit_core: ^0.1.0-dev.0
  flutter:
    sdk: flutter
  meta: ^1.15.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
