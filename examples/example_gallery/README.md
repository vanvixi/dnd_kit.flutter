# dnd_kit Example Gallery

Hosted Flutter web gallery for the repository's runnable `dnd_kit` examples.

Run locally:

```bash
fvm flutter run -d chrome
```

Build for GitHub Pages:

```bash
fvm flutter build web --release --base-href /dnd_kit.flutter/
```
